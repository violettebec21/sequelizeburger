<li>
	{{burger_name}}
    {{#unless devoured}}
	<button class="change-devoured" data-id="{{id}}">
	DEVOUR IT!
	</button>
    {{/unless}}

    {{#if devoured}}
	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
    {{/if}}

</li>